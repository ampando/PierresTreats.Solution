@{
  Layout = "_Layout";
}

@model Treats.Models.Treat

<div class="content">
<h2>Are you sure you want to delete this @Html.DisplayNameFor(model => model.Name)?</h2>

  @using (Html.BeginForm())
  {
    <input class="btn btn-dark" type="submit" value="Delete"/>
  }

  @Html.ActionLink("Back to Treat List", "Index")
</div>